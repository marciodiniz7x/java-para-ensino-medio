public class Matematica {
    public void dividir(int numeroA, int numeroB) {
        int resultado = numeroA / numeroB;
        System.out.printf("%d / %d = %d", numeroA, numeroB, resultado);
    }
}
